export const PUBLIC_ROUTES = ["/de", "/en"];

export const AUTH_ROUTES = ["sign-in", "sign-up"];

export const ERROR_ROUTES = ["/en/auth/error", "/en/auth/verify"];

//prefix for api authentication routes: Routes that start with that prefix are used for api authentication purposes
export const API_AUTH_PREFIX = "/api/auth";

export const DEFAULT_LOGIN_REDIRECT = "/";


